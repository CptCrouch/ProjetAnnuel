using UnityEngine;
using System.Collections;

public class WallDisapear : MonoBehaviour {

    /*BoxCollider boxCollider()
    {
        return GetComponent<BoxCollider>();
    }


	void OnDrawGizmos()
    {
        Gizmos.color = Color.blue;
        Gizmos.DrawWireCube(transform.position + boxCollider().center, boxCollider().size*transform.localScale.y,);
        Gizmos.color = new Color(0,0,1,0.3f);
        Gizmos.DrawCube(transform.position + boxCollider().center, boxCollider().size*transform.localScale.y);
    }*/
}
